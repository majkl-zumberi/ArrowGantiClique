import { MemeItemDirective } from './meme-item.directive';

describe('MemeItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MemeItemDirective();
    expect(directive).toBeTruthy();
  });
});
